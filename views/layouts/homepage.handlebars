<ol class="review-list">
  <li>
    <article class="post">
      <div class="review">
        <a href="{{review_url}}" target="_blank">{{title}}</a>
        <span>({{review_url}})</span>
      </div>
      {{!-- <div class="meta">
        {{vote_count}} point(s) by {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div> --}}
    </article>
  </li>
</ol>