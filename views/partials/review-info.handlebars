
<article class='review m-2'>
    <h3 class='title'>
        {{review_title}}
    </h3>
    <div class='review-details'>
        by {{user.username}} on {{format_date created_at}} | 
        <a href="/single-review/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>

